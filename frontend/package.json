{
  "name": "erp-frontend",
  "version": "1.0.0",
  "description": "Frontend for SAP/Oracle ERP Clone",
  "scripts": {
    "start": "parcel public/index.html",
    "build": "parcel build public/index.html --dist-dir dist"
  },
  "dependencies": {
    "firebase": "^10.0.0"
  },
  "devDependencies": {
    "parcel": "^2.10.0"
  }
}
